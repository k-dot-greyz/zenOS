# keel

ARM-native, Linux-first, f32 DSP kernels.

See `docs/blueprints/keel/` and epic #73.

```
cd keel && cargo test --workspace
```

`keel/rust-toolchain.toml` pins **1.85.0** (edition 2024). Run from `keel/` so rustup picks it up.

Default features must stay GPL-free.
