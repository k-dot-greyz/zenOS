# Runtime / tooling dependencies for zenOS
#
# Source of truth is pyproject.toml (`pip install -e ".[dev]"`); this file is
# a thin mirror for tools that still want `pip install -r requirements.txt`.
# House rule (.github/CI.md): Python floor is >=3.14 — do not add pins below it.
click>=8.0
rich>=13.0
pyyaml>=6.0
jinja2>=3.0
pydantic>=2.0
aiohttp>=3.8
httpx>=0.24.0
python-dotenv>=1.0
prompt-toolkit>=3.0
beautifulsoup4>=4.12.0
schedule>=1.2.0
aiofiles>=23.0
psutil>=5.9

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Security — dependency vulnerability sanitization (zen/setup/troubleshooter.py
# runs this on every `zen setup` / `--validate-only` pass, not just CI)
pip-audit>=2.7

# Optional TTS / media (install as needed)
# pyttsx3>=2.90
# gtts>=2.3.0
# pygame>=2.1.0
# azure-cognitiveservices-speech>=1.19.0
# websockets>=10.0
# pydub>=0.25.1
# librosa>=0.9.0
