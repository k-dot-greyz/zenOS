# env-doctor configuration for zenOS
# See third_party/README.md for env-doctor.sh's provenance/license (GPL-3.0,
# vendored — separate from zenOS's own MIT license).

BRAND="zenOS"

# Python imports env-doctor's Tier-0 scan verifies are actually importable —
# keep in sync with requirements.txt / pyproject.toml [project.dependencies].
ENV_DOCTOR_PYTHON_DEPS="click,rich,yaml,jinja2,pydantic,aiohttp,httpx,dotenv,prompt_toolkit,bs4,schedule,aiofiles,psutil"

ENV_DOCTOR_HELP_URL="https://github.com/k-dot-greyz/zenOS/blob/main/DEV_SETUP_CHEAT_SHEET.md"
